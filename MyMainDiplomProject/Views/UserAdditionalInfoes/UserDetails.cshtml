@model MyMainDiplomProject.Models.UserAdditionalInfo

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <title>
        Інформація про користувача
    </title>
</head>

<body>

    <div class="panel">
        Інформація про користувача
    </div>

    <div class="user-details-container">
        <div class="user-details-block">
            <h3>Аватар користувача</h3>
            <img class="user-image-big" src="@Model.UserAvatar" alt="Avatar" style="max-width: 100%; max-height: 100%;">
        </div>

        <div class="user-details-block">
            <h3>Псевдонім</h3>
            <p>@Model.User.userNikName</p>
        </div>

        <div class="user-details-block">
            <h3>Пошта</h3>
            <p>@Model.User.Email</p>
        </div>
    </div>

    <div class="user-details-container">
        <div class="user-details-block">
            <h3>Дата народження</h3>
            <p>@Model.User.dateOfBirthday</p>
        </div>

        <div class="user-details-block">
            <h3>Дата реєстрації</h3>
            <p>@Model.User.registerDate</p>
        </div>

        <div class="user-details-block">
            <h3>Інформація про себе</h3>
            @if(Model.ShowDescription == true)
            {
                <p>@Model.Description</p>
            }
            else
            {
                <p>Невідомо</p>
            }
        </div>

    </div>

    <div class="user-details-container">
        <div class="user-details-block">
            <h3>Місце роботи</h3>
            @if (Model.ShowWorkPlase == true)
            {
                <p>@Model.WorkPlase</p>
            }
            else
            {
                <p>Невідомо</p>
            }
        </div>

        <div class="user-details-block">
            <h3>Інтереси</h3>
            @if (Model.ShowUserInterests == true)
            {
                <p>@Model.UserInterests</p>
            }
            else
            {
                <p>Невідомо</p>
            }
        </div>

        <div class="user-details-block">
            <h3>Освіта</h3>
            @if (Model.ShowEducation == true)
            {
                <p>@Model.Education</p>
            }
            else
            {
                <p>Невідомо</p>
            }
        </div>
    </div>
</body>